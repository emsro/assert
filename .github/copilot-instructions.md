# Project Instructions

## Building and Running the Project

Always use `cmake --workflow --preset <name>` to build and run the project. Never use bare `cmake --build` or `ctest` in isolation. Available presets are defined in `CMakePresets.json`.
